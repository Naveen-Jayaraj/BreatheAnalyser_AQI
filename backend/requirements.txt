# Core runtime (AQI pipeline + API)
h3>=4.1.0
requests>=2.31.0
fastapi>=0.115.0
uvicorn>=0.30.0
redis>=5.0.0

# Django adapter components bundled in backend package
Django>=5.0
djangorestframework>=3.15.0
celery>=5.4.0

# Optional physics / data processing backends used by pipeline modules
earthengine-api>=0.1.390
pandas>=2.0.0
pyarrow>=15.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0
